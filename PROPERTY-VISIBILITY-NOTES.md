# "나의 매물" vs "전체 공개" 구별하는 방법 설명서

같은 `properties` 테이블(창고)을 두고, **"나의 매물" 페이지(`joinList.tsx`)는 내 것만**, **메인페이지는 모두의 것**을 보여주고 싶을 때 어떻게 구별하는지 순서대로 정리한 문서입니다.

## 1. 왜 이 문제가 생겼을까요?

원래는 RLS(Row Level Security, 행 단위 잠금장치)에 이런 규칙 하나만 있었어요.

```sql
create policy "properties_select_own"
  on public.properties for select
  to authenticated
  using (auth.uid() = owner_id);
```

"로그인한 사람은 **자기 것만** 볼 수 있다"는 규칙이었죠. 그런데 메인페이지에서는 **다른 사람 매물도 보여야** 하니까, 이런 규칙을 하나 더 추가했어요.

```sql
create policy "properties_select_public_all"
  on public.properties for select
  to public
  using (true);
```

"조건 없이 **누구나** 다 볼 수 있다"는 규칙이에요.

### 여기서 문제가 생겼어요

RLS 규칙은 **여러 개 있으면 OR(또는)로 합쳐져요.** 즉:

> "내 것만 보여줘도 되고(규칙1), 또는 전체 다 보여줘도 돼(규칙2)"

둘 중 **하나라도 허락하면 통과**니까, 규칙2(전체 공개)가 생기자마자 사실상 **모든 select 요청에 전체 데이터가 다 보이게** 됐어요. `joinList.tsx`("나의 매물")도 예외 없이 다른 사람 매물까지 다 보이는 사고가 난 거죠.

## 2. 비유로 이해하기

학교 신발장이 있다고 생각해봐요.

- **규칙1(RLS `properties_select_own`)**: "네 이름표 붙은 칸만 열 수 있어"
- **규칙2(RLS `properties_select_public_all`)**: "아무 칸이나 다 열어도 돼" (테스트용으로 자물쇠를 풀어둔 상태)

이 둘이 동시에 걸려있으면, 규칙2 때문에 사실상 **누구나 아무 칸이나 다 열 수 있는 상태**가 돼요. 규칙1이 있어도 소용없어요. 문이 하나라도 열려있으면 열리는 거니까요.

## 3. 그래서 어떻게 구별했을까요? — "DB 잠금"이 아니라 "내가 직접 필터링"

RLS(신발장 자물쇠)만으로는 두 페이지를 구별할 수 없으니까, **"나의 매물" 페이지 쪽 코드에서 직접 "내 것만 주세요"라고 콕 집어서 요청**하도록 바꿨어요.

### Before (구별 안 됨)

```ts
const { data, error } = await supabase.from("properties").select("*");
```

→ "properties 창고에 있는 거 다 줘" (자물쇠가 열려있으니 전부 옴)

### After (나의 매물 페이지)

```ts
const { data, error } = await supabase
  .from("properties")
  .select("*")
  .eq("owner_id", ownerId); // ← 이 줄이 핵심!
```

→ "properties 창고에 있는 것 중에서, `owner_id`가 **나(ownerId)**인 것만 줘"

`.eq("owner_id", ownerId)`의 `eq`는 "equal(같다)"의 줄임말이에요. **"owner_id 칸의 값이 내 아이디랑 똑같은 행만 골라줘"** 라는 필터를 직접 코드에서 거는 거예요.

### 메인페이지는요?

메인페이지는 반대로 **이 필터를 안 걸면 됩니다.** `.eq(...)` 없이 그냥 `.select("*")`만 하면, RLS의 "전체 공개" 규칙 덕분에 모두의 매물이 다 나와요.

| 페이지 | 코드에서 필터를 거나요? | 결과 |
|---|---|---|
| 나의 매물 (`joinList.tsx`) | O (`.eq("owner_id", ownerId)`) | 로그인한 내 것만 |
| 메인페이지 (전체 목록) | X (필터 없음) | 전체 공개 (RLS가 다 열어둠) |

**정리하면:** 자물쇠(RLS)는 이제 "누구나 열 수 있게" 풀어놨고, 대신 **"내 것만 보고 싶은 화면"에서는 코드가 알아서 내 이름표가 붙은 것만 골라 담는** 방식으로 구별한 거예요.

## 4. "내 아이디"는 어디서 가져왔나요?

```ts
const user = useAuth(); // 지금 로그인한 사람 정보

useEffect(() => {
  if (user) {
    getProducts(user.id); // 로그인 정보가 준비되면 그때 내 아이디로 조회
  }
}, [user]);
```

- `useAuth()`는 프로젝트에 이미 만들어져 있던 "지금 로그인한 사람이 누군지 알려주는 기능"이에요.
- 로그인 정보를 확인하는 데 시간이 살짝 걸릴 수 있어서(비동기), `user`가 아직 `null`일 수도 있어요. 그래서 `if (user)`로 **"로그인 정보가 확실히 준비된 다음에만"** 조회하도록 감쌌어요.
- `useEffect`의 두 번째 칸(`[user]`)에 `user`를 넣어서, **로그인 상태가 바뀌면(예: 로그인 완료되는 순간) 자동으로 다시 조회**하게 만들었어요.

## 5. 전체 흐름 한눈에 보기

```
[RLS 정책 2개 존재]
  - properties_select_own      : 내 것만 (authenticated)
  - properties_select_public_all : 전체 공개 (public, 조건 없음)
        ↓ (OR로 합쳐짐 → 사실상 전체 공개 상태)

[나의 매물 페이지]                    [메인페이지 (전체 목록)]
useAuth()로 로그인한 사람 확인          그냥 select("*") 실행
        ↓                                    ↓
.eq("owner_id", user.id) 필터 추가      필터 없음
        ↓                                    ↓
내 매물만 화면에 표시                   모든 사람 매물이 화면에 표시
```

## 6. 나중에 승인 플로우를 추가하면 달라질 부분

지금은 테스트용으로 "전체 공개"(`using (true)`)를 켜놨어요. 나중에 "관리자가 승인한 매물만 메인페이지에 노출"하고 싶어지면, 이 정책 하나만 바꾸면 됩니다.

```sql
-- 전체 공개 대신, 승인된 것만 공개로 변경
using (status = 'published')
```

이렇게 바꿔도 "나의 매물" 페이지 쪽 코드(`.eq("owner_id", ...)`)는 전혀 안 건드려도 돼요. 왜냐하면 그 페이지는 애초에 `owner_id`로 직접 필터링하고 있어서, `status`가 뭐든 상관없이 항상 내 것만 보여주기 때문이에요.
