# 심범경 학습 진단서

지금까지 이 프로젝트를 진행하면서 나눴던 대화들을 근거로, **잘하고 있는 점**과 **더 채워야 할 부분**을 정리했습니다. 추측이 아니라, 실제로 질문했던 내용/겪었던 버그를 근거로 뽑았어요.

## 먼저, 잘하고 있는 점

- **"왜 이렇게 되는 거야?"를 계속 물어보는 습관** — 코드를 그냥 받아쓰지 않고 `...previous`가 뭔지, `createClient()`가 왜 필요한지, RLS 정책이 왜 합쳐지는지 계속 원리를 캐물었어요. 이게 실력이 가장 빨리 느는 방식이에요.
- **콘솔 로그로 직접 증거를 찾는 습관** — "안 되는데?"에서 끝내지 않고, 콘솔 로그/Supabase 테이블 값을 직접 캡처해서 보여주면서 같이 원인을 좁혀나갔어요. 실제로 지오코딩 문제도 이 방식으로 "SDK 준비 안 됨" → "재시도 로직 추가" → "그래도 안 됨" → "실제로 스크립트가 로드되는지 로그 찍기"까지 단계적으로 좁혀갔죠. 이건 실무에서도 그대로 쓰이는 디버깅 방식이에요.
- **단계별로 끊어서 진행하는 방식** — 지도 핀 기능처럼 큰 기능도 "1단계부터 하나씩 해볼게"라고 스스로 쪼개서 진행했어요. 한 번에 다 만들려고 하지 않는 태도가 좋습니다.

## 가장 크게 부족한 부분 (우선순위 순)

### 1순위. React state가 "언제, 어떻게" 반영되는지에 대한 이해

**근거**: `setForm((previous) => ({...previous, address: full}))` 코드를 보고 "이게 다음 버튼 눌러야 저장되는 거냐, 아니면 바로 저장되는 거냐"를 헷갈려 하셨고, `...previous`가 어디서 오는 값인지도 처음엔 몰랐어요.

**왜 중요한가**: 이건 리액트로 뭘 만들든 매번 마주치는 가장 기본적인 개념이에요. 이게 헷갈리면 "왜 화면이 안 바뀌지", "왜 이전 값이 사라지지" 같은 버그를 만들 때마다 원인을 못 찾고 헤매게 돼요.

**공부하면 좋은 순서**:
1. `useState`의 두 번째 반환값(setter)을 호출하면 컴포넌트가 다시 렌더링된다는 것
2. `setState(값)`과 `setState((prev) => 새값)`의 차이 (함수형 업데이트를 왜 쓰는지)
3. 스프레드 문법(`...`)으로 객체를 복사하면서 일부만 바꾸는 패턴 — "불변성(immutability)"이라는 개념
4. React 공식 문서(react.dev)의 "State: A Component's Memory", "State as a Snapshot" 섹션을 읽어보시는 걸 추천해요.

### 2순위. 비동기 처리 (Promise / async-await)와 콜백 기반 API

**근거**: 지오코딩 함수를 `Promise`로 감싸는 이유, `useEffect` 콜백 자체는 `async`로 못 만드는 이유, `handleSave` 안에서 `await`를 왜 여러 번 쓰는지를 하나씩 다시 물어보셨어요.

**왜 중요한가**: Supabase 요청, 이미지 업로드, 카카오 지도 SDK, 위치 정보 API까지 — 이 프로젝트에서 "시간이 걸리는 작업"은 전부 비동기예요. 이 개념이 약하면 "왜 순서가 꼬이지", "왜 아직 준비 안 된 값을 쓰려고 하지" 같은 문제를 계속 만나게 됩니다. 실제로 지오코딩 null 문제도 결국 "비동기 타이밍" 문제였어요.

**공부하면 좋은 순서**:
1. 동기 코드 vs 비동기 코드의 차이 (왜 "기다려야" 하는지)
2. `Promise`가 뭔지 — "나중에 값을 줄게"라는 약속 객체
3. `.then()` 방식과 `async/await` 방식이 사실 같은 걸 다르게 쓰는 거라는 것
4. "콜백 함수만 지원하는 오래된 API"를 `new Promise((resolve) => {...})`로 감싸서 `await` 가능하게 만드는 패턴 (이번에 `getCoordsFromAddress`에서 실제로 썼던 방식)

### 3순위. Next.js App Router의 파일 기반 라우팅 규칙

**근거**: `src/app/join/registration.tsx`라는 **폴더**를 만드는 바람에 실제 라우트가 `/join/registration.tsx`가 되어버려서 `Link href="/join/registration"`이 어긋났던 사건이 있었어요.

**왜 중요한가**: Next.js는 폴더 구조 자체가 URL이 되는 프레임워크라서, 이 규칙을 정확히 모르면 "분명 코드는 맞는데 페이지가 안 뜨는" 버그를 계속 만나게 돼요.

**공부하면 좋은 순서**:
1. `app/` 폴더 안에서 폴더 하나 = URL 경로 하나라는 규칙
2. `page.tsx`, `layout.tsx`가 각각 무슨 역할을 하는지
3. Next.js 공식 문서의 "Routing Fundamentals" 페이지를 한 번 정독해보시는 걸 추천해요.

### 4순위. 데이터베이스 접근 권한(RLS)과 "어디서 필터링해야 하는지" 판단

**근거**: "전체 공개" RLS 정책을 추가했더니 "나의 매물" 페이지에도 다른 사람 매물이 다 보이는 사고가 있었어요. RLS 정책이 여러 개면 **OR로 합쳐진다**는 걸 모르셨던 게 원인이었죠.

**왜 중요한가**: "보안/권한"은 나중에 고치기 훨씬 어려운 영역이에요. 지금은 테스트 단계라 큰 문제가 없었지만, 실서비스였다면 개인정보가 새는 사고로 이어질 수 있는 종류의 실수예요.

**공부하면 좋은 순서**:
1. "인증(로그인했는가)"과 "인가(이걸 볼 권한이 있는가)"는 다른 개념이라는 것
2. Postgres RLS의 기본 동작 — 정책이 여러 개면 OR로 합쳐진다는 것
3. "DB 단에서 막을 것"과 "코드(쿼리)에서 필터링할 것"을 언제 나눠서 써야 하는지 판단 기준

## 지금 당장은 급하지 않지만, 조만간 봐두면 좋은 것

- **`useRef` vs `useState` 차이**: 지도 핀 기능 만들면서 제가 `useRef`를 썼는데, 왜 `useState`가 아니라 `useRef`를 썼는지는 아직 직접 물어보신 적이 없어요. `MAP-PIN-IMPLEMENTATION-NOTES.md`의 2번 항목을 한 번 더 읽어보시길 추천해요.
- **TypeScript 타입 좁히기(type narrowing)**: `latitude: number | null`을 `number`로 좁히는 `filter` 타입가드 코드가 나왔었는데, 이 문법은 처음 보면 낯설 수 있어요. 급하진 않지만 TypeScript를 계속 쓸 거라면 언젠가 정리하고 넘어가면 좋아요.

## 한 줄 요약

**"값이 눈에 보이는 것"과 "값이 실제로 안전하게 저장/보호되는 것"** 사이의 차이를 만드는 개념들(state 반영 시점, 비동기 처리, 라우팅 규칙, DB 권한)이 지금 가장 약한 지점이에요. 반대로 **"원인을 좁혀나가는 디버깅 태도"** 는 이미 잘하고 계셔서, 위 개념들만 채워지면 스스로 문제를 훨씬 빨리 해결하실 수 있을 거예요.
